---
title: "Coordinated Restore at Checkpoint"
description: "Coordinated Restore at Checkpoint (CRaC) is an OpenJDK project for starting Java applications from a checkpoint image, cutting time to first transaction from seconds to milliseconds."
---

<!--
The home page is laid out by layouts/home.html and takes its content from
data/ — the measured numbers from startup.yaml, the runtimes from
runtimes.yaml, the frameworks from frameworks.yaml. There is deliberately no
prose here: the old home page repeated the framework list and the project
description as hand-written HTML, which then had to be kept in step with the
pages that also said it.
-->
